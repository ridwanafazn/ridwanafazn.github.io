---
interface Props {
  text: string;
}

const { text } = Astro.props;
---

<span class="inline-flex items-center px-3 py-1.5 rounded-xl text-[12px] font-mono font-medium bg-primary/5 text-primary border border-primary/20 shadow-sm backdrop-blur-sm">
  {text}
</span>